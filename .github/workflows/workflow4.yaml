name: workflow4
on:
  workflow_dispatch:
jobs:
  invoke_workflow:
    runs-on: ubuntu-latest
    steps:
    - name: some step ( print datetime )
      run: |
        date +"%F_%H-%m-%S"
        echo 'SOME STEP EXECUTED !'
# The End !
